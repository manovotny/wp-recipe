module.exports = function (grunt) {

    'use strict';

    grunt.initConfig({
        config: {
            version: {
                from: '0.1.0',
                to: '0.2.0'
            }
        }
    });

};

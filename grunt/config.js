module.exports = function (grunt) {

    'use strict';

    grunt.initConfig({
        config: {
            version: {
                from: '0.6.1',
                to: '0.6.2'
            }
        }
    });

};

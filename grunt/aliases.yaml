default:
  - build

init:
  - config
  - shell:install
  - update
  - build

config:
  - replace
  - rename

build:
  - clean
  - lint
  - test
  - browserify
  - sass

generate:
  - autoprefixer
  - cssmin
  - unpathify

refresh:
  - shell:refresh

update:
  - shell:update
  - curl
  - build

lint:
  - jshint
  - jscs
  - phplint
  - scsslint

test:
  - phpunit

release:
  - build
  - replace:project_version
  - replace:update_project
  - generate
  - shell:release
  - copy